{
  "name": "battlegrid2",
  "module": "build.ts",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts src/assets/sw.ts --bundle --minify --sourcemap  --outdir=public/build --outbase=src",
    "watch": "npm run build --  --serve --servedir=public"
  },
  "devDependencies": {
    "esbuild": "^0.19.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "yjs": "^13.6.8",
    "lit": "^3.0.0-pre.0"
  }
}